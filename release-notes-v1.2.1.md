# Trench Coat 1.2.1 — Circuit Desk (honesty)

**Codename:** Circuit Desk  
Patch release: versions and public copy match shipped behavior. No new hops, hosts, or attack surface.

## Honesty

- `trench --version`, `pyproject.toml`, package `__version__`, Nexus tagline, landing/AEO, and docs stamps are **1.2.1**.
- README / AEO / `llms.txt`: legal-first **Tor-aware** privacy cloak; explicit **not** a VPN, Tor Browser, I2P client, or crime toolkit.
- Capability table: I2P is stub-only; SS / Hy2 / WG need your client + SOCKS bridge; Tauri is scaffold; telemetry is local-file only.
- Telemetry FAQ no longer implies a network upload.
- SECURITY.md lists 1.2.x as supported.
- Landing encoding (`→`) and Twitter handle (`@suddenlyjon`) aligned with the visible page.

## Unchanged rails

Legal-first only. Fail-closed refuse-direct. `--accept-legal` required to engage. AGPL-3.0-or-later. No new outbound hosts.
